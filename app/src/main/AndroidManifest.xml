<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pyropy.work24">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activities.InsertGig"
            android:parentActivityName=".views.activities.DashboardActivity"
            >

        </activity>
        <activity android:name=".views.activities.VerifyOTP" />
        <activity android:name=".views.activities.SignUp" />
        <activity android:name=".views.activities.Login" />
        <activity android:name=".views.activities.DashboardActivity" />
        <activity android:name=".views.activities.MyJobScreen" />
        <activity android:name=".views.activities.MyGigScreen" />
        <activity android:name=".views.activities.verification" />
        <activity android:name=".views.activities.PasswordRecovery" />
        <activity android:name=".views.activities.Forgotpassword" />
        <activity android:name=".views.activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.MyAppIntroActivity"
            android:label="My Custom AppIntro" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>